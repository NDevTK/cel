package: infra/celab/third_party/chocolatey_packages/chocolatey_core_extension
description: the nupkg that Chocolatey uses to install Chocolatey core extension offline.
# IMPORTANT:
# After uploading a new sysinternals package in CIPD, please remember to run
# the 'cipd set-ref' command to set the package version number and 'latest'
# ref on the new CIPD package instance.
install_mode: copy
# This CIPD package should contain a single nupkg or a single nuspec file.
# Official source for downloading sysinternals:
# https://www.nuget.org/packages/rb.chocolatey-core.extension/
data:
  - file: rb.chocolatey-core.extension.1.3.3.nupkg