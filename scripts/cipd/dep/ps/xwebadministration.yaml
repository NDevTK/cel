package: infra/celab/third_party/powershell_modules/xwebadministration
# Celab uses this nupkg install the xWebAdministration Powershell module on
# a test client machine.
# IMPORTANT: After uploading a new package, please remember to run the
# "cipd set-ref" command to add the package version as a ref.
description: the Powershell module xactivedirectory nupkg.
install_mode: copy
data:
  - file: xwebadministration.2.2.0.nupkg