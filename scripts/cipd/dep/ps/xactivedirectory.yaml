package: infra/celab/third_party/powershell_modules/xactivedirectory
# Celab uses this nupkg install the XActiveDirectory Powershell module on a
# test client machine.
# IMPORTANT: After uploading a new package, please remember to run the
# "cipd set-ref" command to add the package version as a ref.
description: the XActiveDirectory nupkg.
install_mode: copy
data:
  - file: xactivedirectory.2.19.0.nupkg