package: infra/celab/third_party/powershell_modules/xcomputermanagement
# Celab uses this nupkg install the XComputerManagement Powershell module on
# a test client machine.
# IMPORTANT: After uploading a new package, please remember to run the
# "cipd set-ref" command to add the package version as a ref.
description: the XComputerManagement nupkg.
install_mode: copy
data:
  - file: xcomputermanagement.4.1.0.nupkg