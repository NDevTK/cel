package: infra/celab/third_party/pip-packages/pywin32
description: files for installing the pywin32 package offline.
# IMPORTANT:
# After uploading a pip package in CIPD, please remember to run the
# 'cipd set-ref' command to set the package version number and 'latest'
# ref on the new CIPD package instance.
install_mode: copy
# This package instance should contain the following files:
# - An 'pywin32.txt' file (note the HARD-CODED file name).that specifies the
#   name of the pywin32 package and any of its dependencies, as well as a
#   version for each package.
# - Wheels or zips for each package.
data:
  - file: pywin32.txt
  - file: pywin32-227-cp27-cp27m-win_amd64.whl