.DS_Store
.idea/*
*.pyc

# Local build environment file. This file is evaluated as a Python literal and
# must evaluate to a dict whose keys are environment variables and whose values
# are strings that will be assigned to those environment variables during
# builds.
.build.environment

# Generated files.
*.pb.go
go/gcp/compute/validate.go
go/tools/gen_doc_proto/testdata/test.pb
schema/gcp/compute/compute-api.proto

# Vendored dependencies. Use `dep` to manage.
vendor/

# Build artifacts.
out/
